# 墨潮读书进度记录

- 记录时间：2026-04-11
- 记录目的：保存今天的线上修复、部署调整、项目规划梳理结果，方便明天继续。

## 一、今天已经完成的工作

### 1. 线上站点恢复

- 已确认 `dushu.my` 与 `dushumy.pages.dev` 当前可访问。
- 今日已排查并修复“站点挂掉”的核心问题：域名实际指向的是 Cloudflare Pages，而不是旧的 Workers Static Assets 发布链路。
- 已手动重新部署 Pages，并验证站点恢复。

### 2. 自动部署链路修复

已完成并推送以下修复：

- `ef4ff85` `fix: 切换到 Cloudflare Pages 部署`
  - GitHub Actions 从旧的 Workers 部署改为 Cloudflare Pages 部署。
  - 已补齐 `CLOUDFLARE_API_TOKEN` 与 `CLOUDFLARE_ACCOUNT_ID` 的 GitHub Secrets。
- `fd8afb2` `chore: 启用 GitHub Actions Node 24 运行时`
  - 先临时让 Actions 跑在 Node 24 环境下。
- `c70edc7` `chore: 清理 Actions Node 20 弃用告警`
  - 将 `actions/checkout` 升到 `v5`
  - 将 `actions/setup-node` 升到 `v5`
  - 去掉 `cloudflare/wrangler-action@v3`
  - 改为直接执行 `npx wrangler@4.81.1 pages deploy dist --project-name=dushumy`
  - 最新 workflow 已成功，且 Node 20 弃用告警已消失。

### 3. 当前线上验证结果

- `https://dushu.my/` 返回 `HTTP 200`
- `https://dushumy.pages.dev/` 返回 `HTTP 200`
- 当前线上主站已恢复正常。

## 二、今天查到的历史规划情况

### 1. 旧项目上下文来源

- 旧工作目录 `/var/code/dushu` 现在已经不存在。
- 但已从 Claude 会话历史目录中恢复到大量上下文：
  - `~/.claude/projects/-private-var-code-dushu`

### 2. 已确认的历史事实

Claude 当时的主计划，是把旧的纯 HTML 模板站改造成 Astro 6 SSG 的公版书阅读站，核心内容包括：

- Astro 6 静态站架构
- Content Collections
- 首页 / 分类页 / 作者页 / 书籍页 / 章节页 / 搜索页 / 版权页 / 404
- Pagefind 搜索
- 推荐算法
- 构建验证与部署

这条“基础建站”主线，现在已经完成。

### 3. 历史计划里仍未推进完的事项

从当前仓库 `TODOS.md` 看，旧计划里仍明确留着两个未完成项：

- 技术验证：大规模页面构建性能验证（原来是为未来 50K 页面量级预留）
- 法律验证：内容源使用协议 / 版权链路验证

这两个事项，在进入大规模内容扩张前仍然重要。

## 三、当前项目内容现状

截至今天整理时，当前仓库内容规模约为：

- 书籍：5 本
- 章节文件：229 个

当前已存在书目：

- 红楼梦
- 三国演义
- 西游记
- 水浒传
- 金瓶梅

所以当前站点已经不是最初的 demo 骨架，而是进入“可以继续扩馆藏”的阶段。

## 四、今天和用户已经确认的下一阶段方向

今天已经与用户确认，下一阶段目标为：

### 1. 总方向

- 目标：把网站继续完善成“像一个免费的小型读书馆”
- 优先级：`内容扩充优先`

### 2. 第一轮馆藏目标

用户已确认：

- 第一轮目标规模：约 `20` 本
- 馆藏路线：`古典综合馆`
- 内容策略：`先把馆藏铺开成型`，优先做到“书目、简介、目录、部分正文”齐备

### 3. 已确认的推荐推进方案

已确认采用：

- `方案 2：内容 + 馆藏展示一起推进`

也就是：

- 一边扩充书目与章节
- 一边升级首页 / 分类页 / 作者页 / 专题入口
- 最终让网站更像“馆”，而不是“几本书的 demo”

### 4. 已确认的第一轮 20 本馆藏方向

已在今天的设计讨论中确认这类分层思路：

- 核心长篇名著
- 历史演义
- 神魔奇幻
- 世情讽刺
- 公案侠义
- 志怪笔记
- 话本短篇集
- 生活 / 古文入口型书目

并已得到用户认可。

### 5. 已确认的完成度标准

今天已确认一套内容完成度标准：

- `L1`：书目可见（元数据、书籍页、分类、作者、目录、版权说明齐）
- `L2`：可试读（具备一批正文内容）
- `L3`：可连续阅读（核心馆藏达到较强阅读体验）
- `L4`：完整收录（长期目标）

当前建议的第一轮验收线为：

- 20 本全部至少达到 `L1`
- 其中 5 本达到 `L3`
- 另外 5 本达到 `L2`

这个标准也已获得用户认可。

## 五、今天做到但还没有继续推进的事项

### 1. `www.dushu.my` 301 跳转

今天中途讨论过：

- 用户已同意把 `www.dushu.my` 做 `301 永久跳转` 到 `https://dushu.my`

但该事项没有正式做完，原因是会话后续转入了“下一阶段产品规划”。

当前状态：

- `www.dushu.my` 还没有完成最终跳转配置
- Cloudflare 动态跳转 rulesets API 在当前 token 下返回过未授权，需要明天决定：
  - 是改用更高权限 token
  - 还是改走其他 Cloudflare 重定向配置方式

### 2. `workflow_dispatch` 手动部署入口

今天已经在本地 `/.github/workflows/deploy.yml` 中加入了：

- `workflow_dispatch`

但该改动尚未单独整理、提交、推送。

## 六、当前工作区状态

当前仓库不是干净状态，除今天的规划外，还有其他本地内容改动存在。

当前可见未提交项包括：

- `/.github/workflows/deploy.yml`（本地含 `workflow_dispatch` 变更）
- `/CLAUDE.md`
- `/scripts/`
- `/src/content/books/jinpingmei.json`
- `/src/content/books/shuihuzhuan.json`
- `/src/content/chapters/jinpingmei/`
- `/src/content/chapters/shuihuzh/`

明天继续时要注意：

- 不要误覆盖这些未提交内容
- 先区分哪些是今天的部署/规划遗留，哪些是之前已存在的内容扩充工作

## 七、建议明天早上继续的顺序

建议明天按下面顺序继续：

1. 先读本文件 `docs/progress.md`
2. 确认是否先补完基础收尾：
   - `www.dushu.my` 的 301 跳转
   - `workflow_dispatch` 的提交与推送
3. 然后正式进入“第一轮 20 本古典综合馆”执行规划
4. 第一件实质工作建议是：
   - 先把 20 本正式书单定稿
   - 再给每本书标注目标完成度（L1 / L2 / L3）
   - 再拆成可执行批次

## 八、明天恢复时的建议提示词

如果明天继续，可以直接从这个方向接上：

> 继续墨潮读书项目，先读取 `docs/progress.md`，然后按“20 本古典综合馆、内容扩充优先、方案 2”继续往下规划和执行。

---

## 2026-04-12 工作续记

- 记录时间：2026-04-12
- 记录目的：承接昨天的“方案 2”，把核心 3 本补到可连续阅读，并把今天的验证结果留档。

### 一、今天完成的核心内容推进

#### 1. 核心 3 本已补齐到前 12 章

今天已完成以下 3 本核心馆藏的章节扩充：

- `hongloumeng`
- `sanguoyanyi`
- `xiyouji`

处理结果：

- 原本这 3 本各只有前 `1-3` 章
- 现已全部补齐到前 `12` 章
- 对应章节目录现都已有 `12` 个 Markdown 文件，可作为连续试读入口

#### 2. 核心 3 本真实完成度已提升到 `L3`

已更新以下文件中的真实馆藏状态：

- `src/content/books/hongloumeng.json`
- `src/content/books/sanguoyanyi.json`
- `src/content/books/xiyouji.json`

更新结果：

- `hongloumeng`: `L2 -> L3`
- `sanguoyanyi`: `L2 -> L3`
- `xiyouji`: `L2 -> L3`

这意味着当前第一轮馆藏里的“核心长篇入口”已经更接近之前确认的阅读体验目标。

### 二、今天完成的抓取链路增强

#### 1. 新增核心 3 本抓取规则

已在抓取规则里补齐以下书目的维基文库来源配置：

- `hongloumeng`
- `sanguoyanyi`
- `xiyouji`

确认结果：

- 这 3 本在维基文库均采用 `第001回` 这一类三位回目标号
- 本地章节文件也统一采用三位编号，如 `001.md`

#### 2. 改进章节标题与正文清洗

为保证抓取后的章节页面可直接用于站内阅读，今天又补了两类解析能力：

- 支持从《红楼梦》这种 `{{center|'''第四回 ... '''}}` 居中模板中提取标题
- 支持把《西游记》`header.section` 中的 `<br>` 标记清理成纯文本标题

同时还补了正文清洗规则，避免把以下噪音带进正文：

- 上一回 / 下一回 / 回目录导航
- `----` 这类分隔线噪音

### 三、当前馆藏状态小结

截至今天收工前：

- 首轮正式书单仍为 `20` 本
- 重点 5 本已保持前 `12` 篇试读，真实完成度为 `L2`
- 核心 3 本现已补齐到前 `12` 章，真实完成度提升为 `L3`

如果按之前确认的分层目标来看，核心深读入口已经继续往前推进了一步。

### 四、今天的验证结果

今天最后一次完整验证结果如下：

- `bun run test`
  - `40` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`373` pages
- `bun run build:verify`
  - 构建验证：`776` 项检查全部通过

### 五、下次继续时建议接着做的事

明天恢复时，建议优先看这里，然后继续下面几件事：

1. 继续扩充剩余重点馆藏的真实正文覆盖，逐步把“5 本 L2”继续向更稳定的试读质量推进
2. 继续完善分类页 / 专题页 / 作者页的内容策展文案，让站点更像“小型免费读书馆”
3. 评估下一批书目是否要从 `L1 -> L2`，优先补那些最能提升首页观感和分类完整度的书

### 六、明天恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-12 已完成的核心 3 本扩充结果，然后继续把剩余馆藏和站内策展做完整。

---

## 2026-04-13 工作续记

- 记录时间：2026-04-13
- 记录目的：继续执行“方案 2”，把分类页、专题页、作者页从基础聚合页补成更像读书馆导览页。

### 一、今天完成的策展页增强

#### 1. 分类页已补成“分馆导览页”

今天已增强分类页的数据和页面结构，新增了三组策展信息：

- `适合谁读`
- `入馆路线`
- `邻馆串逛`

对应变化：

- `src/data/library/categories.ts` 已为全部分馆补齐适读人群、入馆顺序和相邻分馆联动
- `src/pages/category/[slug].astro` 已把这些数据真实渲染到页面

这让分类页不再只是“按分类列书”，而是更像馆员在入口处给读者做导览。

#### 2. 专题页已补成“策展说明页”

今天为专题数据和专题页新增了三组内容：

- `策展缘起`
- `阅读路线`
- `相关分馆`

对应变化：

- `src/data/library/topics.ts` 已为当前专题补齐策展说明、推荐阅读路径和关联分馆
- `src/pages/topic/[slug].astro` 已把专题从“专题书单页”升级为“带导览说明的策展页”

这样专题页终于不只是堆一排卡片，而是有了“为什么把这些书放在一起”的解释。

#### 3. 作者页已补成“作者展架页”

今天对作者资料做了两件事：

- 把作者展架资料扩充到当前全部入藏作者
- 给作者页新增导览字段和展示区块

作者页新增的内容块包括：

- `馆内定位`
- `气质标签`
- `从哪本开始`
- `顺手再逛`

对应变化：

- `src/data/library/authors.ts` 已覆盖当前全部作者 slug，不再只限 3 位作者
- `src/pages/author/[slug].astro` 已能展示每位作者在馆内的定位、入口作品与延伸去向

这一步解决了很多作者页此前仍在使用“文案正在整理中”兜底文案的问题。

### 二、今天补的工程保障

#### 1. 已补设计文档与执行计划

为避免后续策展增强继续散开，今天把这轮工作单独落档：

- `docs/superpowers/specs/2026-04-13-curation-pages-design.md`
- `docs/superpowers/plans/2026-04-13-curation-pages.md`

#### 2. 已补测试卡口

今天新增并通过了两类校验：

- `tests/library-data.test.ts`
  - 锁定分类、专题、作者新增策展字段
  - 锁定作者资料已覆盖当前全部入藏作者
- `tests/build-verify.ts`
  - 锁定分类页、专题页、作者页必须出现新的策展区块
  - 锁定冯梦龙作者页等不再落回兜底文案

### 三、今天最后一次验证结果

今天最后一次完整验证结果如下：

- `bun run test`
  - `40` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`373` pages
- `bun run build:verify`
  - 构建验证：`787` 项检查全部通过

### 四、下次继续建议

明天如果继续，建议优先做下面两类工作之一：

1. 继续补首页、搜索页、分类总览页的策展文案联动，让整站入口语言更统一
2. 继续把剩余 `L1` 馆藏往 `L2` 推，优先补最能增强首页观感与分类完整度的作品

### 五、明天恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-13 已完成的分类页、专题页、作者页策展增强，再继续补首页导览或推进下一批馆藏内容。

## 2026-04-13 工作续记（入口导览增强）

- 记录时间：2026-04-13
- 记录目的：把首页、搜索页、分类总览页也补成“能回答第一次来该怎么逛”的入口页，打通整站导览语言。

### 一、今天完成的入口导览增强

#### 1. 首页已补上第一次进站的导览结构

今天为首页新增了三组明确的入口区块：

- `怎么逛这座小馆`
- `按兴趣进入`
- `馆藏地图`

对应变化：

- 新增 `src/data/library/entry-guides.ts`
  - 集中管理首页、搜索页、分类总览页的导览文案和跳转关系
- 更新 `src/pages/index.astro`
  - 让首页不只展示书架，也能先告诉读者“应该从哪种方式进入”

现在首页已经能回答三个关键问题：

- 第一次来应该先从哪里逛
- 如果我知道自己喜欢什么，应该点哪个入口
- 站内现在有哪些主要入口结构

#### 2. 搜索页已补上“不知道搜什么”的兜底导览

今天为搜索页新增了两组导览区块：

- `如果你不知道搜什么`
- `按读法找书`

对应变化：

- 更新 `src/pages/search.astro`
  - 在搜索框前增加专题、分馆、读法三种导览方式

这样搜索页不再只适合“已经知道关键词的人”，也开始能接住“我只是想找个入口看看”的读者。

#### 3. 分类总览页已补成真正的“选馆页”

今天为分类总览页新增了两组全局导览：

- `怎么选馆`
- `推荐逛法`

同时每张分馆卡片也新增了：

- `适合谁`

对应变化：

- 更新 `src/pages/category/index.astro`
  - 在分馆列表前补了选馆说明和推荐路线
  - 在每张分馆卡里补上适读人群与入馆建议

这样分类总览页不再只是“馆名列表”，而是更像真正的馆内总地图。

### 二、今天补的工程整理

#### 1. 已补设计文档与执行计划

这轮入口导览增强也已单独落档：

- `docs/superpowers/specs/2026-04-13-entry-guidance-design.md`
- `docs/superpowers/plans/2026-04-13-entry-guidance.md`

#### 2. 已补数据与样式层整理

今天新增或调整了这些关键文件：

- `src/data/library/entry-guides.ts`
  - 收拢三个入口页的导览数据，避免文案散落在页面模板里
- `src/styles/global.css`
  - 补了导览卡片、导览标签、路线列表等增量样式

#### 3. 已补构建校验卡口

- `tests/build-verify.ts`
  - 锁定首页必须出现 `怎么逛这座小馆`、`按兴趣进入`、`馆藏地图`
  - 锁定搜索页必须出现 `如果你不知道搜什么`、`按读法找书`
  - 锁定分类总览页必须出现 `怎么选馆`、`推荐逛法`、`适合谁`

### 三、今天最后一次验证结果

今天这轮入口导览增强的最后一次完整验证如下：

- `bun run test`
  - `40` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`373` pages
- `bun run build:verify`
  - 构建验证：`795` 项检查全部通过

### 四、现在的站点状态

截至今天收工前，站点已经具备以下入口层结构：

- 首页：能解释“第一次来怎么逛”
- 搜索页：能解释“我不知道搜什么怎么办”
- 分类总览页：能解释“我该先选哪一馆”
- 分类页：能解释“进馆后该怎么读”
- 专题页：能解释“为什么把这些书放在一起”
- 作者页：能解释“从这位作者哪本开始”

也就是说，站点的“馆入口语言”已经基本打通，不再只是若干页面各自为战。

### 五、下次继续建议

明天如果继续，建议优先做下面两类工作之一：

1. 继续把剩余重点馆藏从 `L1` 往 `L2` / `L3` 推，优先补能明显增强首页书架观感的作品
2. 继续补搜索体验和专题联动，例如搜索推荐词、专题互链、作者入口联动等

### 六、明天恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-13 已完成首页、搜索页、分类总览页的入口导览增强，再继续推进馆藏内容补充或下一轮导览联动。

## 2026-04-13 工作续记（短篇馆藏 L2 与搜索专题联动）

- 记录时间：2026-04-13
- 记录目的：继续把短篇阅读线补成真正可逛的入口，同时把搜索页、专题页、作者入口联动做实。

### 一、今天完成的短篇馆藏推进

#### 1. 5 本短篇组已从 `L1` 提升到 `L2`

今天已把以下 5 本短篇组的真实完成度统一提升到 `L2`：

- `阅微草堂笔记`
- `子不语`
- `喻世明言`
- `警世通言`
- `醒世恒言`

对应变化：

- 更新了 5 本书籍 JSON 的 `completionLevel`
- 同时补齐了对应 `featuredTopics`
  - `阅微草堂笔记`、`子不语` 继续挂到 `strange-tales`
  - `喻世明言`、`警世通言`、`醒世恒言` 挂到 `three-yans`

这一步意味着站内的“夜读短篇 / 白话短篇”已经不再只是书目占位，而是进入真实可试读状态。

#### 2. 已补齐 5 本短篇组的首批章节内容

今天已用抓取脚本把首批章节真实落库：

- `阅微草堂笔记`：前 `8` 卷
- `子不语`：前 `8` 卷
- `喻世明言`：前 `10` 卷
- `警世通言`：前 `10` 卷
- `醒世恒言`：前 `10` 卷

对应变化：

- 新增章节目录：
  - `src/content/chapters/yueweicaotangbiji/`
  - `src/content/chapters/zibuyu/`
  - `src/content/chapters/yushimingyan/`
  - `src/content/chapters/jingshitongyan/`
  - `src/content/chapters/xingshihengyan/`
- `tests/library-data.test.ts` 已锁定这 5 本至少具备首批可试读章节

这样短篇线已经有了一批真实正文，不再只是“准备要补”的状态。

#### 3. 已补齐抓取规则与新专题数据

今天还完成了两类底层数据整理：

- `scripts/fetch-chapter-helpers.mjs`
  - 已补齐上述 5 本短篇书的维基文库抓取规则
- `src/data/library/topics.ts`
  - 新增专题 `three-yans`（三言话本）
  - 新增 `relatedTopicSlugs`
  - 新增 `featuredAuthorSlugs`

其中 `three-yans` 已正式收拢：

- `喻世明言`
- `警世通言`
- `醒世恒言`

这让“三言”终于从零散单书，变成站内可承接流量的专题入口。

### 二、今天完成的搜索页与专题页联动

#### 1. 搜索页已升级成可直接进入的前台

今天继续增强 `src/pages/search.astro`，新增了三组入口：

- `热门搜词`
- `从作者进入`
- `从专题进入`

首批已接入的内容包括：

- 热门搜词：`志怪`、`话本`、`短篇`、`夜读`、`冯梦龙`
- 作者入口：`冯梦龙`、`纪昀`、`袁枚`
- 专题入口：`志怪传奇`、`三言话本`、`世情小说`

这样搜索页现在不再只是“你先自己想到词再来搜”，而是能先给读者一批可直接点击的入口。

#### 2. 搜索页已支持 `/search?q=关键词` 深链接

今天已在搜索页接入 `?q=` 查询参数：

- 页面初始化 `PagefindUI` 后，会读取 `window.location.search`
- 如果存在 `q`，就自动触发对应关键词搜索

对应变化：

- 搜索页里的热门搜词卡片会直接跳到 `/search?q=<关键词>`
- 作者入口和专题入口中的推荐词，也都能复用这个深链接能力

这意味着后续首页、专题页、作者页都可以继续复用搜索深链接，而不需要重新做一套“推荐词搜索”逻辑。

#### 3. 三言专题页已补上“相关专题 / 作者入口”

今天已增强 `src/pages/topic/[slug].astro`，让专题页可以真实读取：

- `relatedTopicSlugs`
- `featuredAuthorSlugs`

对应页面新增区块：

- `相关专题`
- `作者入口`

同时还补了 `src/lib/library.ts` 的辅助函数，统一读取：

- 专题关联专题
- 专题关联作者

此外，`src/data/library/authors.ts` 中的冯梦龙资料也已补挂到 `three-yans`，让“专题 -> 作者 -> 专题”的入口链更完整。

### 三、今天补的文档与测试卡口

#### 1. 已补本轮 spec / plan

本轮短篇馆藏与搜索联动的设计和计划已经落档：

- `docs/superpowers/specs/2026-04-13-shortform-l2-discovery-linkage-design.md`
- `docs/superpowers/plans/2026-04-13-shortform-l2-discovery-linkage.md`

#### 2. 已补新的数据测试与构建校验

今天新增并转绿了这些检查：

- `tests/library-data.test.ts`
  - 锁定 5 本短篇组已提升到 `L2`
  - 锁定三言专题存在
  - 锁定 5 本短篇组已有首批章节
- `tests/chapter-fetch-plan.test.ts`
  - 锁定 5 本短篇书的抓取规则格式
- `tests/build-verify.ts`
  - 锁定搜索页必须出现 `热门搜词`、`从作者进入`、`从专题进入`
  - 锁定 `dist/topic/three-yans.html` 存在
  - 锁定三言专题页必须出现 `相关专题`、`作者入口`

### 四、今天最后一次验证结果

今天这轮短篇馆藏与搜索专题联动的最后一次完整验证如下：

- `bun run test`
  - `48` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`420` pages
- `bun run build:verify`
  - 构建验证：`895` 项检查全部通过

另外已额外确认：

- `src/pages/search.astro` 中已接入 `URLSearchParams(...).get('q')`
- 构建产物 `dist/search.html` 中已包含 `/search?q=` 深链接入口和 `triggerSearch(...)` 钩子

### 五、现在的站点状态

截至今天这一轮完成后，站内的短篇阅读线已经具备以下入口结构：

- 志怪线：可从 `志怪传奇 -> 纪昀 / 袁枚 -> 搜索词` 进入
- 话本线：可从 `三言话本 -> 冯梦龙 -> 搜索词` 进入
- 搜索页：可从 `热门词 / 作者 / 专题` 三种方式进入

也就是说，站点现在已经不只是“有短篇书”，而是开始真正形成“短篇也有自己的逛法”。

### 六、下次继续建议

下次如果继续，建议优先做下面两类工作之一：

1. 继续把这 5 本短篇组从当前首批可试读，往更稳的 `L2` / `L3` 推进，优先补更多卷数与命中率更高的篇目
2. 继续扩搜索推荐词、专题互链和作者入口到更多书系，例如神魔、公案、历史演义等路线

### 七、下次恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-13 已完成的短篇馆藏 L2 推进、三言专题、搜索页热门搜词与专题作者联动，再继续补下一批馆藏或扩更多搜索入口。

---

## 2026-04-13 工作续记（历史风云 / 神魔奇想入口联动）

- 记录时间：2026-04-13
- 记录目的：把搜索页、专题页、作者页与书目数据继续往“历史演义线 / 神魔小说线”扩一轮，让入口联动从首页导览真正落到具体书系。

### 一、今天完成的第二波入口联动

#### 1. 新增 2 个可直达的专题入口

今天新增了两个专题页入口：

- `historical-epics` / `历史风云`
- `mythic-realms` / `神魔奇想`

对应变化：

- `src/data/library/topics.ts` 已补齐两个专题的名称、策展说明、阅读路线、作者入口与相关专题
- 新专题已正式出现在站内专题体系中，不再只停留在分类或作者维度

这一步让“历史演义”和“神魔奇想”不再只是书目标签，而是读者可直接进入的策展入口。

#### 2. 搜索页已补齐历史线 / 神魔线导览入口

今天继续补强了搜索页中“如果你不知道搜什么”的入口数据。

新增的热门搜词包括：

- `权谋`
- `三国`
- `神魔`
- `封神`
- `妖怪`

新增的作者入口包括：

- `罗贯中`
- `吴承恩`
- `许仲琳`

新增的专题入口包括：

- `历史风云`
- `神魔奇想`

对应变化：

- `src/data/library/entry-guides.ts` 已把历史线 / 神魔线搜索导览接入现有三组结构：
  - 热门搜词
  - 从作者进入
  - 从专题进入

现在搜索页已经能更自然地接住“我想看三国”“我想看神魔妖怪故事”这类模糊需求。

#### 3. 作者页、专题页、书籍数据已形成双向互链

今天把历史线与神魔线涉及到的作者、专题、书籍关系也补齐了。

作者页回链已补到：

- 历史线作者回链 `historical-epics`
- 神魔线作者回链 `mythic-realms`

对应作者包括：

- `罗贯中`
- `吴承恩`
- `许仲琳`

对应挂入新专题的书目包括：

- 历史线
  - `sanguoyanyi`
  - `dongzhoulieguozhi`
  - `suitangyanyi`
  - `shuoyuequanzhuan`
- 神魔线
  - `xiyouji`
  - `fengshenyanyi`
  - `jinghuayuan`

另外今天还修复了专题互链的对称性问题：

- `worldly-novels` 已回链 `historical-epics`
- `strange-tales` 已回链 `mythic-realms`

这一步补完后，站内“搜索 -> 专题 -> 作者 -> 书籍”的跳转关系更完整，不再出现单向能到、反向回不去的情况。

### 二、今天补的工程保障

#### 1. 已补设计文档与执行计划

这轮“历史 / 神魔入口联动”也已单独落档：

- `docs/superpowers/specs/2026-04-13-history-myth-entry-linkage-design.md`
- `docs/superpowers/plans/2026-04-13-history-myth-entry-linkage.md`

#### 2. 已补测试与构建校验

今天先补红灯，再把数据层联通到位，相关卡口包括：

- `tests/library-data.test.ts`
  - 锁定新专题已存在
  - 锁定作者与专题互链
  - 锁定对应书目已真实挂到新专题
  - 锁定搜索页入口数据已补齐
- `tests/build-verify.ts`
  - 锁定搜索页必须出现 `历史风云`、`神魔奇想`
  - 锁定专题页产物必须成功生成
  - 锁定专题页必须出现 `相关专题` 与 `作者入口`

同时，本轮还做过一次专题互链质量复查，并在修正后通过复审。

### 三、今天最后一次验证结果

今天这轮“历史风云 / 神魔奇想”入口联动的最后一次完整验证如下：

- `bun run test`
  - `52` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`422` pages
- `bun run build:verify`
  - 构建验证：`910` 项检查全部通过

### 四、下次继续建议

如果下一轮继续扩馆，建议优先从下面两类工作里选一类：

1. 继续扩“公案侠义 / 英雄传奇”这一波入口联动，让更多书系接入搜索页、专题页、作者页的策展通路
2. 回头继续把首批短篇馆藏从当前试读状态往更稳的 `L2 / L3` 推，优先补最能提升阅读连续性的作品

### 五、下次恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-13 已完成的“历史风云 / 神魔奇想”第二波入口联动，再继续扩公案侠义入口，或继续补短篇馆藏的正文深度。

---

## 2026-04-13 工作续记（公案英雄入口联动与短篇正文加深）

- 记录时间：2026-04-13
- 记录目的：把“公案侠义 / 英雄传奇”这一条热血线接入搜索页、专题页、作者页，同时把 5 本短篇组从首批试读继续补到更稳的连续阅读深度。

### 一、今天完成的公案侠义 / 英雄传奇入口联动

#### 1. 新增 2 个可直接承接流量的专题入口

今天新增了两个专题页入口：

- `gong-an-heroics` / `公案侠义`
- `heroic-tales` / `英雄传奇`

对应变化：

- `src/data/library/topics.ts` 已补齐两个专题的策展说明、阅读路线、相关专题与作者入口
- 站内现在已经不只剩历史、神魔、世情、短篇能以专题形式承接，热血通俗线也有了自己的入口

其中：

- `公案侠义` 当前收拢 `三侠五义`
- `英雄传奇` 当前收拢 `水浒传`、`说岳全传`、`隋唐演义`

这一步补完后，`公案`、`包公`、`梁山`、`岳飞` 这些高识别度兴趣词，终于不再只能靠单书或分类页承接。

#### 2. 搜索页已补上公案 / 英雄第三波入口

今天继续扩了搜索页入口数据。

新增的热门搜词包括：

- `包公`
- `展昭`
- `公案`
- `梁山`
- `好汉`
- `岳飞`

新增的作者入口包括：

- `石玉昆`
- `施耐庵`
- `钱彩`

新增的专题入口包括：

- `公案侠义`
- `英雄传奇`

对应变化：

- `src/data/library/entry-guides.ts` 已把这条热血线接入现有三组结构：
  - 热门搜词
  - 从作者进入
  - 从专题进入

现在搜索页已经能同时接住：

- 想看断案节奏的读者
- 想看梁山好汉和江湖群像的读者
- 想从岳飞忠义线切入英雄传奇的读者

#### 3. 作者页、专题页、书籍 JSON 已补齐互链

今天把这轮相关作者、专题、书籍之间的关系也补齐了。

作者页回链已补到：

- `石玉昆 -> gong-an-heroics`
- `施耐庵 -> heroic-tales`
- `钱彩 -> heroic-tales`
- `褚人获 -> heroic-tales`

书籍 JSON 已真实挂到新专题：

- `sanxiawuyi -> gong-an-heroics`
- `shuihuzhuan -> heroic-tales`
- `shuoyuequanzhuan -> heroic-tales`
- `suitangyanyi -> heroic-tales`

同时还顺手补了专题之间的回链对称性：

- `four-masterpieces` 已可分流到 `heroic-tales`
- `historical-epics` 已可分流到 `heroic-tales`
- `three-yans` 已可分流到 `gong-an-heroics`

这样现在站内已经能形成更顺畅的跳转链路：

- 搜索 -> 专题 -> 作者 -> 书籍
- 四大名著 / 历史风云 -> 英雄传奇
- 三言话本 -> 公案侠义

### 二、今天完成的短篇正文加深

#### 1. 5 本短篇组都已补到至少 12 篇

今天继续用现有抓取链路补齐了以下目录：

- `阅微草堂笔记`：`8 -> 12`
- `子不语`：`8 -> 12`
- `喻世明言`：`10 -> 12`
- `警世通言`：`10 -> 12`
- `醒世恒言`：`10 -> 12`

新增章节目录文件包括：

- `src/content/chapters/yueweicaotangbiji/09.md` 到 `12.md`
- `src/content/chapters/zibuyu/09.md` 到 `12.md`
- `src/content/chapters/yushimingyan/11.md` 到 `12.md`
- `src/content/chapters/jingshitongyan/11.md` 到 `12.md`
- `src/content/chapters/xingshihengyan/11.md` 到 `12.md`

这一步补完后，短篇线已经不再只是“每本先摆几篇”，而开始有更像一整排书架的阅读厚度。

#### 2. 短篇组已形成 `L2 / L3` 分层

今天没有把 5 本短篇全部一口气写成 `L3`，而是先立了两本更扎实的锚点：

- `阅微草堂笔记`：`L2 -> L3`
- `喻世明言`：`L2 -> L3`

继续保持 `L2` 的有：

- `子不语`
- `警世通言`
- `醒世恒言`

这样短篇线现在已经形成两种更明确的角色：

- `阅微草堂笔记`：志怪笔记入口的 `L3` 锚点
- `喻世明言`：三言话本入口的 `L3` 锚点
- 其余三本：继续保持稳定 `L2`，但正文深度已一起补厚

### 三、今天补的文档与工程保障

#### 1. 已补本轮设计文档与执行计划

这轮工作也已单独落档：

- `docs/superpowers/specs/2026-04-13-heroic-entry-shortform-depth-design.md`
- `docs/superpowers/plans/2026-04-13-heroic-entry-shortform-depth.md`

#### 2. 已补并转绿的测试卡口

今天先补红灯，再把两条工作一起转绿：

- `tests/library-data.test.ts`
  - 锁定公案侠义 / 英雄传奇专题存在
  - 锁定作者与专题互链
  - 锁定书籍 JSON 已真实挂到新专题
  - 锁定搜索页已接入公案 / 英雄第三波入口
  - 锁定 5 本短篇都达到至少 `12` 篇
  - 锁定 `阅微草堂笔记`、`喻世明言` 提升到 `L3`
- `tests/build-verify.ts`
  - 锁定搜索页必须出现 `公案侠义`、`英雄传奇`、`石玉昆`、`施耐庵`、`钱彩`
  - 锁定 `dist/topic/gong-an-heroics.html`
  - 锁定 `dist/topic/heroic-tales.html`
  - 锁定两个新专题页必须出现 `相关专题` 与 `作者入口`

### 四、今天最后一次验证结果

今天这轮“公案英雄入口联动 + 短篇正文加深”的最后一次完整验证如下：

- `bun run test`
  - `56` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`438` pages
- `bun run build:verify`
  - 构建验证：`953` 项检查全部通过

### 五、下次继续建议

明天如果继续，建议优先从下面两类工作里选一类：

1. 继续把 `三侠五义`、`说岳全传`、`隋唐演义` 往真实试读推进，让今天新立起来的公案 / 英雄入口也具备更强正文承接力
2. 继续把短篇线从当前 `12` 篇的稳定试读，往更强的连续阅读体验推进，优先补 `子不语`、`警世通言`、`醒世恒言`

### 六、下次恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-13 已完成的公案侠义 / 英雄传奇入口联动，以及 5 本短篇补到至少 12 篇、两本短篇锚点升到 L3，再继续推进公案 / 英雄正文承接，或继续补短篇正文深度。

---

## 2026-04-14 工作续记（公案与英雄三书试读推进）

- 记录时间：2026-04-14
- 记录目的：继续承接昨天新立起来的 `公案侠义 / 英雄传奇` 入口，把 `三侠五义`、`说岳全传`、`隋唐演义` 三本真正补到可试读状态，让入口开始有正文承接力。

### 一、今天完成的公案 / 英雄正文承接

#### 1. 三本都已补到前 12 回

今天已为以下三本建立真实章节目录，并统一补到前 `12` 回：

- `三侠五义`
- `说岳全传`
- `隋唐演义`

对应新增章节目录：

- `src/content/chapters/sanxiawuyi/001.md` 到 `012.md`
- `src/content/chapters/shuoyuequanzhuan/001.md` 到 `012.md`
- `src/content/chapters/suitangyanyi/001.md` 到 `012.md`

这一步补完后，昨天刚建立的两个入口专题终于不再只是“点进去有卡片”，而是已经能接住真实阅读。

#### 2. 三本真实完成度都已提升到 `L2`

今天已更新以下三本书籍 JSON：

- `src/content/books/sanxiawuyi.json`
- `src/content/books/shuoyuequanzhuan.json`
- `src/content/books/suitangyanyi.json`

更新结果：

- `sanxiawuyi`: `L1 -> L2`
- `shuoyuequanzhuan`: `L1 -> L2`
- `suitangyanyi`: `L1 -> L2`

这意味着：

- `公案侠义` 线已经不再只是馆藏占位
- `英雄传奇` 线里除《水浒传》外，也开始有了另外两本真正可试读的承接作品

### 二、今天补的抓取规则

#### 1. 已补三本的维基文库抓取配置

今天在 `scripts/fetch-chapter-helpers.mjs` 中补齐了三本的抓取规则：

- `sanxiawuyi`
- `shuoyuequanzhuan`
- `suitangyanyi`

确认结果：

- `三侠五义`：采用 `第001回` 这一类三位数字回目
- `说岳全传`：采用 `第一回`、`第十二回` 这类中文数字回目
- `隋唐演义`：采用 `/001` 这类三位数字子页面

这样当前公案 / 英雄线的抓取链路已经不再只覆盖 `水浒传`，而是能稳定接住三本新增试读作品。

### 三、今天补的文档与测试卡口

#### 1. 已补本轮设计文档与执行计划

这轮工作也已单独落档：

- `docs/superpowers/specs/2026-04-14-heroic-trial-reading-design.md`
- `docs/superpowers/plans/2026-04-14-heroic-trial-reading.md`

#### 2. 已补并转绿的测试

今天先补红灯，再转绿：

- `tests/chapter-fetch-plan.test.ts`
  - 锁定三本抓取规则存在
  - 锁定文件名格式与源链接格式正确
- `tests/library-data.test.ts`
  - 锁定三本章节目录都至少达到前 `12` 回
  - 锁定三本 `completionLevel` 已提升到 `L2`

### 四、今天最后一次验证结果

今天这轮“公案与英雄三书试读推进”的最后一次完整验证如下：

- `bun run test`
  - `62` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`474` pages
- `bun run build:verify`
  - 构建验证：`1025` 项检查全部通过

### 五、下次继续建议

如果下一轮继续，建议优先从下面两类工作里选一类：

1. 继续把 `三侠五义`、`说岳全传`、`隋唐演义` 从前 `12` 回往更稳的连续阅读推进，优先决定这三本里谁先冲 `L3`
2. 回头继续补短篇线，优先把 `子不语`、`警世通言`、`醒世恒言` 从当前稳定试读继续往更强阅读体验推进

### 六、下次恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-14 已完成的公案与英雄三书试读推进，再继续决定是先把三侠五义 / 说岳 / 隋唐里的某一本冲到 L3，还是回头继续补短篇线。

---

## 2026-04-14 工作续记（短篇三书补厚到前 16 篇）

- 记录时间：2026-04-14
- 记录目的：承接今天早些时候已经完成的公案 / 英雄正文推进，回头继续补短篇线，把 `子不语`、`警世通言`、`醒世恒言` 三本从稳定试读再补厚一层，但仍保持 `L2` 口径不变。

### 一、今天完成的短篇三书补厚

#### 1. 三本都已从前 12 篇补到前 16 篇

今天继续复用现有抓取链路，为以下三本补齐了第 `13-16` 篇：

- `子不语`
- `警世通言`
- `醒世恒言`

对应新增章节文件：

- `src/content/chapters/zibuyu/13.md` 到 `16.md`
- `src/content/chapters/jingshitongyan/13.md` 到 `16.md`
- `src/content/chapters/xingshihengyan/13.md` 到 `16.md`

补完后，这三本当前的真实试读深度已变为：

- `zibuyu`: `12 -> 16`
- `jingshitongyan`: `12 -> 16`
- `xingshihengyan`: `12 -> 16`

这一步的意义不是再立新入口，而是把原本已经有入口的短篇支撑书真正补厚，让短篇线整排书的观感更均衡。

#### 2. 三本真实完成度继续保持 `L2`

今天没有把这三本直接提升到 `L3`，而是继续保持：

- `src/content/books/zibuyu.json` -> `L2`
- `src/content/books/jingshitongyan.json` -> `L2`
- `src/content/books/xingshihengyan.json` -> `L2`

这样当前短篇线的分层仍然清晰：

- `阅微草堂笔记`、`喻世明言` 继续作为更深的短篇锚点
- `子不语`、`警世通言`、`醒世恒言` 作为更厚、更稳的 `L2` 支撑书

### 二、今天补的文档与测试卡口

#### 1. 已补本轮设计文档与执行计划

这轮“短篇三书补厚”也已单独落档：

- `docs/superpowers/specs/2026-04-14-shortform-l2-depth-balance-design.md`
- `docs/superpowers/plans/2026-04-14-shortform-l2-depth-balance.md`

#### 2. 已补并转绿的测试

今天按 TDD 先补红灯，再转绿：

- `tests/library-data.test.ts`
  - 把 `zibuyu`、`jingshitongyan`、`xingshihengyan` 的目标章节数从 `12` 提升到 `16`
  - 继续锁定三本 `completionLevel` 必须保持 `L2`

红灯确认点：

- 首次运行局部测试时，明确失败在 `zibuyu` 章节数仍只有 `12`，未达到 `16`

### 三、今天最后一次验证结果

今天这轮“短篇三书补厚到前 16 篇”的最后一次完整验证如下：

- `bun run test`
  - `62` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`486` pages
- `bun run build:verify`
  - 构建验证：`1049` 项检查全部通过

### 四、下次继续建议

下一轮如果继续，建议优先从下面两类工作里选一类：

1. 在 `子不语`、`警世通言`、`醒世恒言` 三本里选一本继续往 `L3` 推，先立一个新的短篇深读强入口
2. 回到公案 / 英雄线，决定 `三侠五义`、`说岳全传`、`隋唐演义` 里谁先从前 `12` 回继续往更强连续阅读推进

### 五、下次恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-14 已完成的短篇三书补厚结果，再决定是让短篇三书里的一本先冲 L3，还是回到公案 / 英雄线继续加深正文承接。

---

## 2026-04-14 工作续记（三侠五义推进到 L3）

- 记录时间：2026-04-14
- 记录目的：在今天已完成的公案 / 英雄三书前 `12` 回试读基础上，先把最适合做门面入口的 `三侠五义` 单独继续往前推进，建立公案侠义线的第一个 `L3` 深读入口。

### 一、今天完成的《三侠五义》加深推进

#### 1. 《三侠五义》已从前 12 回补到前 20 回

今天继续复用既有抓取链路，为 `三侠五义` 补齐了第 `13-20` 回：

- `src/content/chapters/sanxiawuyi/013.md` 到 `020.md`

补完后，《三侠五义》当前真实试读深度已变为：

- `sanxiawuyi`: `12 -> 20`

这样现在 `公案侠义` 线不再只是“有一本能试读”，而是已经开始出现更像主打书的连续阅读入口。

#### 2. 《三侠五义》真实完成度已提升到 `L3`

今天已更新：

- `src/content/books/sanxiawuyi.json`

更新结果：

- `sanxiawuyi`: `L2 -> L3`

同时本轮明确保持不变：

- `shuoyuequanzhuan`: 继续保持前 `12` 回、`L2`
- `suitangyanyi`: 继续保持前 `12` 回、`L2`

这样公案 / 英雄线当前已经形成更明确的分层：

- `三侠五义`：先作为 `公案侠义` 线的 `L3` 深读入口
- `说岳全传`、`隋唐演义`：继续作为 `L2` 试读承接书

### 二、今天补的文档与测试卡口

#### 1. 已补本轮设计文档与执行计划

这轮《三侠五义》加深推进也已单独落档：

- `docs/superpowers/specs/2026-04-14-sanxiawuyi-l3-depth-design.md`
- `docs/superpowers/plans/2026-04-14-sanxiawuyi-l3-depth.md`

#### 2. 已补并转绿的测试

今天按 TDD 先补红灯，再转绿：

- `tests/library-data.test.ts`
  - 把公案 / 英雄三书从“统一前 `12` 回、统一 `L2`”改成分层目标
  - 锁定 `sanxiawuyi >= 20`
  - 锁定 `sanxiawuyi.completionLevel === 'L3'`
  - 锁定 `shuoyuequanzhuan`、`suitangyanyi` 继续保持前 `12` 回与 `L2`

红灯确认点：

- 首次运行局部测试时，明确失败在 `sanxiawuyi` 章节数仍只有 `12`、完成度仍为 `L2`
- 补完正文后再次运行，失败点收敛为 `sanxiawuyi` 尚未升到 `L3`
- 最后提升完成度后，局部测试全部转绿

### 三、今天最后一次验证结果

今天这轮“《三侠五义》推进到 L3”的最后一次完整验证如下：

- `bun run test`
  - `62` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`494` pages
- `bun run build:verify`
  - 构建验证：`1065` 项检查全部通过

### 四、下次继续建议

下一轮如果继续，建议优先从下面两类工作里选一类：

1. 回到 `说岳全传`、`隋唐演义` 中挑一本继续往 `L3` 推，把英雄传奇线也立起第二个更强入口
2. 回到短篇线，在 `子不语`、`警世通言`、`醒世恒言` 三本里选一本继续从 `L2` 冲到 `L3`

### 五、下次恢复时可直接使用的提示词

> 继续墨潮读书项目，先读取 `docs/progress.md`，确认 2026-04-14 已完成的《三侠五义》L3 推进结果，再决定是继续推说岳 / 隋唐里的下一本，还是回到短篇线再立一个新的 L3 入口。

---

## 2026-04-14 工作续记（说岳全传推进到 L3）

- 记录时间：2026-04-14
- 记录目的：承接今天已经完成的《三侠五义》L3 推进，继续把英雄传奇线里最适合承接连续阅读的《说岳全传》也往前补深，并顺手修正章节标题抽取质量问题。

### 一、今天完成的《说岳全传》加深推进

#### 1. 《说岳全传》已从前 12 回补到前 20 回

今天继续复用既有抓取链路，为 `说岳全传` 补齐了第 `13-20` 回：

- `src/content/chapters/shuoyuequanzhuan/013.md` 到 `020.md`

补完后，《说岳全传》当前真实试读深度已变为：

- `shuoyuequanzhuan`: `12 -> 20`

这样现在公案 / 英雄线不再只有《三侠五义》一条更深入口，而是已经形成双书可连续追读的门面。

#### 2. 《说岳全传》真实完成度已提升到 `L3`

今天已更新：

- `src/content/books/shuoyuequanzhuan.json`

更新结果：

- `shuoyuequanzhuan`: `L2 -> L3`

同时本轮明确保持不变：

- `sanxiawuyi`: 继续保持前 `20` 回、`L3`
- `suitangyanyi`: 继续保持前 `12` 回、`L2`

这样公案 / 英雄线当前已经形成更明确的分层：

- `三侠五义`：`L3`
- `说岳全传`：`L3`
- `隋唐演义`：`L2`

#### 3. 已顺手修复《说岳全传》章节标题抽取问题

今天还一起修了抓取时的章节标题识别：

- `scripts/fetch-chapters.mjs`
  - 已支持从单行 `{{Header|...|section=...}}` 模板中提取真实回目标题
- `src/content/chapters/shuoyuequanzhuan/001.md` 到 `020.md`
  - frontmatter 标题已批量修正，不再落成 `詩曰：` 之类假标题

### 二、今天补的文档与测试卡口

#### 1. 已补本轮设计文档与执行计划

这轮《说岳全传》加深推进也已单独落档：

- `docs/superpowers/specs/2026-04-14-shuoyuequanzhuan-l3-depth-design.md`
- `docs/superpowers/plans/2026-04-14-shuoyuequanzhuan-l3-depth.md`

#### 2. 已补并转绿的测试

今天按 TDD 先补红灯，再转绿：

- `tests/library-data.test.ts`
  - 锁定 `shuoyuequanzhuan >= 20`
  - 锁定 `shuoyuequanzhuan.completionLevel === 'L3'`
  - 明确公案 / 英雄三书当前是双 `L3` / 单 `L2` 分层
- `tests/chapter-fetch-plan.test.ts`
  - 新增单行 `Header|section=` 标题抽取测试
  - 防止《说岳全传》标题再次回退成 `詩曰：`

### 三、当时最后一次验证结果

这轮“《说岳全传》推进到 L3”完成后的完整验证如下：

- `bun run test`
  - `64` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`502` pages
- `bun run build:verify`
  - 构建验证：`1081` 项检查全部通过

---

## 2026-04-14 工作续记（阅读器与 EPUB 上线闭环）

- 记录时间：2026-04-14
- 记录目的：把站点从“能看目录和正文”继续收口到更像小型免费读书馆的可上线状态，补齐阅读器基本体验和 EPUB 下载能力。

### 一、今天完成的产品能力补齐

#### 1. 章节页已补齐阅读器核心能力

今天已完成章节阅读页的基础阅读器闭环：

- 阅读设置已支持并持久化到 `localStorage`
  - 字体大小
  - 行距
  - 版心宽度
  - 主题切换（默认 / 护眼 / 夜间）
- 阅读进度会自动保存
- 支持“恢复上次进度”
- 支持为当前章节保存 / 移除书签
- 支持查看当前书的最近阅读和书签列表

对应主要代码：

- `src/components/ReaderControls.astro`
- `src/lib/reader-state.ts`
- `src/pages/book/[slug]/[chapter].astro`

#### 2. 书籍页已补齐继续阅读与书签入口

今天已在书籍详情页补齐面向回访读者的入口：

- `继续阅读`
  - 会优先跳到该书最近一次阅读到的章节
- `下载 EPUB`
  - 提供直接离线下载入口
- `最近阅读`
  - 显示当前书最近读过的章节
- `我的书签`
  - 显示当前书已保存的书签

对应主要代码：

- `src/components/BookReaderHub.astro`
- `src/pages/book/[slug]/index.astro`

#### 3. 站点已具备静态 EPUB 下载能力

今天已新增静态 EPUB 构建链路：

- `src/lib/epub.ts`
  - 负责把书籍元数据与章节正文组装成标准 EPUB 包
- `src/pages/epub/[slug].epub.ts`
  - 在构建期直接输出静态 EPUB 文件

当前已确认构建出可下载的 EPUB 路径，例如：

- `/epub/hongloumeng.epub`
- `/epub/sanguoyanyi.epub`
- `/epub/xiyouji.epub`
- `/epub/sanxiawuyi.epub`
- `/epub/shuoyuequanzhuan.epub`

同时其他已落正文的书也都会一并生成对应 EPUB。

### 二、今天补的测试卡口

今天新增并转绿的测试包括：

- `tests/reader-state.test.ts`
  - 锁定阅读设置归一化
  - 锁定进度保存
  - 锁定历史去重与上限
  - 锁定书签切换
- `tests/epub.test.ts`
  - 锁定 EPUB 基础结构
  - 锁定章节 XHTML 与目录文件生成
- `tests/build-verify.ts`
  - 新增书籍页“继续阅读 / 最近阅读 / 我的书签 / 下载 EPUB”校验
  - 新增章节页“阅读设置 / 阅读进度 / 保存书签 / 最近阅读 / 恢复上次进度”校验
  - 新增构建产物中 EPUB 文件存在性与体积校验

### 三、今天最后一次验证结果

今天这轮“阅读器与 EPUB 上线闭环”的最后一次完整验证如下：

- `bun run test`
  - `73` 个测试全部通过
- `bun run build`
  - Astro 构建成功
  - 产物页数：`502` pages
- `bun run build:verify`
  - 构建验证：`1100` 项检查全部通过

### 四、下一步上线收口建议

如果下一步继续直奔上线，建议顺序如下：

1. 提交并推送当前工作区里这批站点代码与内容改动
2. 触发 GitHub Actions 的 Pages 部署
3. 部署完成后复查：
   - `https://dushu.my/`
   - 任意一本书页的 `继续阅读`
   - 任意一本书页的 `下载 EPUB`
   - 任意一章的进度保存 / 书签 / 历史

### 五、同日补充：线上真实阅读流程走查修复

在部署上线后又做了一轮线上真实阅读流程走查，额外修了两处会影响回访体验的问题：

- 修复章节页离开时阅读进度被意外写回 `0%` 的问题
  - 根因是页面卸载阶段强制落盘再次从 DOM 测量进度，浏览器切页瞬间会把测量值打回 `0`
  - 现已在 `src/lib/reader-state.ts` 新增强制落盘进度决策函数，并在 `src/components/ReaderControls.astro` 接入
- 补齐站点图标文件
  - 新增 `public/favicon.svg`
  - 修复线上浏览器控制台 `favicon.svg 404`

对应补充测试：

- `tests/reader-state.test.ts`
  - 新增强制落盘进度不应异常回退到 `0` 的断言
- `tests/build-verify.ts`
  - 新增 `favicon.svg` 产物校验

这轮补修后的本地验证结果：

- `bun run test`：`75` 个测试全部通过
- `bun run build`：成功，`502` pages
- `bun run build:verify`：`1101` 项检查全部通过

### 六、同日补充：书页进度回显再次收口

继续做线上真实阅读链路复测时，发现还有一处“章节页看起来是 100%，回到书页却显示 0%”的残留问题，本轮已继续收口：

- 根因补充确认
  - 章节页滚动保存用了 `180ms` 延迟定时器
  - 在点击“返回目录”或切到后台时，强制落盘虽然已经保住正确进度，但先前挂起的延迟定时器仍可能随后执行
  - 该定时器会在页面隐藏态重新测到 `0`，再把 `localStorage` 里的正确进度覆盖掉，最终导致书页“上次进度 / 最近阅读”回显成 `0%`
- 本轮修复
  - `src/lib/reader-state.ts`
    - `resolvePersistedReadingProgress(...)` 新增隐藏态保护
    - 页面隐藏后若延迟落盘测到 `0`，会保留已有有效进度
  - `src/components/ReaderControls.astro`
    - 延迟落盘执行前先判断 `document.hidden`
    - 在 `beforeunload` 与 `visibilitychange` 里清掉挂起定时器，再做强制落盘
- 新增测试
  - `tests/reader-state.test.ts`
    - 新增“页面进入隐藏态后，延迟落盘不应把已保存进度冲回 0”断言

这轮本地验证结果：

- `bun run test`：`76` 个测试全部通过
- `bun run build`：成功，`502` pages
- `bun run build:verify`：`1101` 项检查全部通过

### 七、同日补充：新增“我的阅读”聚合页

为了让站点更像一个能回访、能续读的小型读书馆，本轮继续补了一页面向读者自己的聚合入口：

- 新增 `我的阅读` 页面
  - 路由：`/reading`
  - 顶部导航已补入口：`我的阅读`
  - 页面会集中展示：
    - `继续阅读中的书`
    - `最近书签`
    - `最近足迹`
    - `当前阅读设置`
- 数据来源
  - 直接读取本机浏览器 `localStorage` 中的 `mochao-reader-state`
  - 不依赖后端，可随当前静态站一起部署
- 本轮新增代码
  - `src/components/ReadingDashboard.astro`
    - 新增聚合页组件
    - 可直接跳回章节、回到书页、下载 EPUB
  - `src/pages/reading.astro`
    - 新增独立页面
  - `src/layouts/Base.astro`
    - 顶部导航新增 `我的阅读`
  - `src/lib/reader-state.ts`
    - 新增 `getReadingDashboardSnapshot(...)`
    - 用于聚合在读书目、最近书签、最近历史与阅读设置
- 本轮新增测试
  - `tests/reader-state.test.ts`
    - 新增阅读总览聚合与排序断言
  - `tests/build-verify.ts`
    - 新增 `/reading.html` 存在性与关键模块校验

这轮本地验证结果：

- `bun run test`：`77` 个测试全部通过
- `bun run build`：成功，`503` pages
- `bun run build:verify`：`1108` 项检查全部通过

这轮上线验证结果：

- 已推送提交：`2cc9717 feat: 新增我的阅读聚合页`
- GitHub Actions 部署：`24387992100`
  - 结果：`success`
- 线上复测：`https://dushu.my/reading`
  - 无阅读记录时，空状态文案正常
  - 人工写入一份示例阅读状态后，页面已正确显示：
    - `继续阅读中的书`
    - `最近书签`
    - `最近足迹`
    - `当前阅读设置`
  - “继续阅读 / 回到书页 / 下载 EPUB” 按钮均已生成正确链接
